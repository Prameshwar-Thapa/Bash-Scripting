[ "$a" = "$b" ]     # Equal
[ "$a" != "$b" ]    # Not equal
[ -z "$a" ]         # Empty string
[ -n "$a" ]         # Non-empty string
[ "$a" \< "$b" ]    # Less than (lexical)
[ "$a" \> "$b" ]    # Greater than (lexical)
